#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"

echo "🧪 Docs: adding headers…"
bash "$ROOT_DIR/scripts/add-doc-headers.sh" "$(date +%F)"

echo "🧪 Docs: validating tags & duplicates…"
bash "$ROOT_DIR/scripts/validate-docs.sh"

echo "🧪 Docs: checking links…"
python3 "$ROOT_DIR/scripts/check-links.py"

echo "✅ Docs checks passed"



